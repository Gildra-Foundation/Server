---
extends: default

rules:
  line-length:
    max: 100
    level: warning
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
  document-start: disable
  empty-lines:
    max-end: 1
